SELECT Id, Email, DisplayName, Role, Repos, MaxProjects FROM Users ORDER BY Id;
SELECT Id, Name, Slug, Domain, RepoFullName, DatabaseName, IisSiteName, Status, CreatedByUserId FROM Projects ORDER BY Id;
